apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production
nameSuffix: -production

commonLabels:
  app.kubernetes.io/name: 01-manual-production

resources:
  - ../../base

patchesStrategicMerge:
  - replicas.yaml
  - service.yaml

images:
  - name: ghcr.io/lukma99/gitops-test-env
    newTag: 3.0.0